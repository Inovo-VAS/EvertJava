CREATE TABLE <DBUSER>.RESPIN_SERVICE_REQUEST(
	ID NUMERIC(18,0) IDENTITY(1,1) NOT NULL,
	REQUESTORIP VARCHAR(100) NOT NULL,
	SERVICEID NUMERIC(18,0) NOT NULL,
	SERVICENAME VARCHAR(1000),
	RECORDSAFFECTED NUMERIC(18,0) DEFAULT 0,
	RECORDSNOTAFFECTED NUMERIC(18,0) DEFAULT 0,
	REQUESTINITSTAMP DATETIME DEFAULT GETDATE(),
	REQUESTTARTSTAMP DATETIME DEFAULT GETDATE(),
	REQUESTCOMPLETEDSTAMP DATETIME DEFAULT GETDATE(),
	REQUESTDURATION NUMERIC(18,0) DEFAULT 0,
	RESPINCONFIRMATIONLABEL VARCHAR(1000) DEFAULT 'INITIATED',
	CREATESTAMP DATETIME DEFAULT GETDATE(),
	CONSTRAINT PK_RESPIN_SERVICE_REQUEST PRIMARY KEY(
		ID ASC
	)
)

CREATE TABLE <DBUSER>.RESPIN_SERVICE_REQUEST_LOADS(
	ID NUMERIC(18,0) IDENTITY(1,1) NOT NULL,
	RESPINREQUESTID NUMERIC(18,0) NOT NULL,
	SERVICEID NUMERIC(18,0),
	LOADID NUMERIC(18,0),
	LOADNAME VARCHAR(1000),
	CREATESTAMP DATETIME DEFAULT GETDATE(),
	CONSTRAINT PK_RESPIN_SERVICE_REQUEST_LOADS PRIMARY KEY(
		ID ASC
	)
)

CREATE TABLE <DBUSER>.RESPIN_SERVICE_REQUEST_QCODES(
	ID NUMERIC(18,0) IDENTITY(1,1) NOT NULL,
	RESPINREQUESTID NUMERIC(18,0) NOT NULL,
	SERVICEID NUMERIC(18,0),
	QCODE NUMERIC(18,0),
	QCODEDESCRIPTION VARCHAR(1000),
	CREATESTAMP DATETIME DEFAULT GETDATE(),
	CONSTRAINT PK_RESPINE_SERVICE_REQUEST_QCODES PRIMARY KEY(
		ID ASC
	)
)

CREATE TABLE <DBUSER>.RESPIN_SERVICE_REQUEST_RESPINLEADS(
	ID NUMERIC(18,0) IDENTITY(1,1) NOT NULL,
	RESPINREQUESTID NUMERIC(18,0) NOT NULL,
	SERVICEID NUMERIC(18,0),
	LOADID NUMERIC(18,0),
	SOURCEID NUMERIC(18,0),
	LASTQCODE NUMERIC(18,0),
	CREATESTAMP DATETIME DEFAULT GETDATE(),
	CONSTRAINT PK_RESPIN_SERVICE_REQUEST_RESPINLEADS PRIMARY KEY(
		ID ASC
	)
)

